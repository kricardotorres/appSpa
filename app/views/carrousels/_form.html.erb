<%= form_with(model: carrousel, local: true) do |form| %>
  <% if carrousel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(carrousel.errors.count, "error") %> prohibited this carrousel from being saved:</h2>

      <ul>
      <% carrousel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :display_from %>
    <%= form.date_select :display_from %>
  </div>

  <div class="field">
    <%= form.label :display_finish %>
    <%= form.date_select :display_finish %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
